
# Input = block, bottom information (both position and shape)
# Output = change shape?
# Activation function = tanh, ranking how good it does from -1 to 1
# Population size = how many running for each generation
# Fitness function = score, how many row is remove, to determine which one is better
# Max generation = 30
